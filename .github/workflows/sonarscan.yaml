name: SonarQube
on: push
jobs:
  run_qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./install_sonarscanner.sh
      - run: sonar-scanner
        timeout-minutes: 5